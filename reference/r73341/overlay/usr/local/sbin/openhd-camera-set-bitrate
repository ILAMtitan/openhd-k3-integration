#!/usr/bin/env bash
set -euo pipefail
[[ $# -eq 1 && "$1" =~ ^[0-9]+$ ]] || { echo "Usage: $0 bits-per-second" >&2; exit 2; }
sed -i "s/^OPENHD_CAMERA_BITRATE=.*/OPENHD_CAMERA_BITRATE=$1/" /etc/default/openhd-ti-camera
systemctl restart openhd-ti-camera-bridge.service
